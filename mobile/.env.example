# Android emulator: use 10.0.2.2 to reach the host machine.
# Physical device: replace 10.0.2.2 with the LAN IP of the computer running Flask.
# iOS simulator: localhost usually points to the host machine.
EXPO_PUBLIC_API_URL=http://10.0.2.2:5000/api/v1
